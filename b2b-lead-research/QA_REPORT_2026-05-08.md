# B2B Lead Research — QA Test Report
**Date:** Friday, May 8th, 2026 — 11:45 AM (Asia/Colombo)  
**Tester:** QA Testing Agent (cron:22288711-2de5-4734-bb46-aa43a61bf2f0)  
**Commit:** No commits yet (untracked working tree)

---

## 1. Git Status
| Item | Status |
|------|--------|
| Branch | `master` |
| Commits | **None** — repo is uninitialized |
| Changes | All files untracked |
| **Severity** | **INFO** |

> **Note:** The repository has no commit history. Consider running `git init && git add . && git commit -m "Initial commit"` to establish version control.

---

## 2. Pytest Results

### Summary
- **Total Tests:** 93
- **Passed:** 90
- **Failed:** 3
- **Warnings:** 38 (all `DeprecationWarning: datetime.datetime.utcnow()`)
- **Duration:** 3.33s

### ❌ Failed Tests (Root Cause Analysis)

#### 1. `tests/test_enrichment.py::TestWebsiteEnricher::test_analyze_design`
```
assert result["has_schema_markup"] is True
assert False is True
```
- **Root Cause:** The test passes `html = "display: flex;"` to `_analyze_design()`, but expects `has_schema_markup=True`. The schema markup (`application/ld+json`) is only in the `soup` object, not in the `html` parameter.
- **Fix:** Update test to include `"application/ld+json"` or `"schema.org"` in the `html` string passed to `_analyze_design()`.
- **Severity:** **WARNING** (test bug, not code bug)

#### 2. `tests/test_pipeline.py::TestLeadPipeline::test_score_leads_handles_errors`
```
assert result[0]["website_quality_score"] == 50
assert 38.0 == 50
```
- **Root Cause:** The test expects a default score of 50 for an empty lead, but `ScoringEngine.calculate()` produces 38.0 for a lead with all `None` fields. No exception is thrown, so the fallback `score=50` in the error handler is never reached.
- **Fix:** Either change test expectation to 38, or mock `scorer.calculate()` to raise an exception to actually test error handling.
- **Severity:** **WARNING** (test bug)

#### 3. `tests/test_scoring.py::TestScoringEngine::test_calculate_full`
```
assert score == 100
assert 95 == 100
```
- **Root Cause:** Test uses `page_speed_score=85`, which scores 10 points (threshold for 15 points is ≥90). All other metrics sum to 85, giving total 95.
- **Fix:** Change `page_speed_score` to 95 (or any ≥90) to achieve perfect 100, or update assertion to 95.
- **Severity:** **WARNING** (test bug)

---

## 3. Dashboard Status

| Check | Result |
|-------|--------|
| HTTP Status | `200 OK` |
| Process | Running (PID 164907) |
| Port | 8501 |
| Health | `ok` |
| Data Loaded | None (no valid output files) |

- **Severity:** **INFO**
- Dashboard loads successfully but shows "No lead data found" because pipeline hasn't produced real data yet.

### Dashboard Code Issues
| Issue | Location | Severity |
|-------|----------|----------|
| Type hint mismatch: `load_data() -> pd.DataFrame` but returns `(pd.DataFrame, str, int)` tuple | `dashboard/app.py:85` | **INFO** |
| `df_raw.empty` checked but `load_data` can return tuple causing `AttributeError` risk | `dashboard/app.py:99` | **WARNING** |

---

## 4. API Collector Tests

### Google Places Collector
- **Scrape mode:** Initialized OK
- **Sample query:** `coffee shop London` → **0 results**
- **Analysis:** Scrape mode parses Google Maps HTML with regex. Google's HTML structure changes frequently, making scraping brittle. This is expected behavior for a fallback mechanism.
- **Severity:** **INFO**

### Companies House Collector
- **Status:** Not tested (no API key configured)
- **Severity:** **INFO**

### PageSpeed Insights
- **Status:** Not tested (no API key configured)
- **Severity:** **INFO**

---

## 5. Pipeline End-to-End Test

```bash
$ python -c "from pipeline import LeadPipeline; p = LeadPipeline(); p.process_batch(country='UK', batch_size=2)"
```

**Result:** `None` (no leads discovered)

**Reason:** No API keys in environment (`GOOGLE_PLACES_API_KEY`, `COMPANIES_HOUSE_API_KEY` not set).

**Behavior:** Gracefully handles missing keys — no crashes, returns `None` with appropriate log warnings.

**Excel Output:** Existing file `output/b2b_ecom_research_UK_001_20260508.xlsx` contains 1 test row with 28/29 columns as `NaN`. Data types are incorrect (string columns like `record_id`, `country` show as `float64` due to pandas NaN coercion).

- **Severity:** **INFO** (expected without API keys)

---

## 6. Performance & Resource Analysis

### Memory Test
- **Scenario:** 1,000 scoring calculations + 100 enricher initializations + 500 cache entries
- **Memory Delta:** ~464 KB
- **Conclusion:** No memory leak detected. Objects are garbage-collected properly.

### Blocking Operations
Multiple `time.sleep()` calls found (expected for polite crawling):
- `pipeline.py`: 2 calls (2s delays between API calls)
- `discovery/google_places.py`: 5 calls (2s pagination delays, 0.5s scrape politeness)
- `discovery/companies_house.py`: 1 call (0.5s rate limiting)
- `enrichment/website_analyzer.py`: 1 call (crawl delay enforcement)

**Recommendation:** For high-throughput scenarios, consider async I/O or thread pools. Current design is safe but slow for large batches.

### SQLite Audit Database
- **Size:** 36 KB
- **Indexes:** Present on `url`, `record_id` (both tables)
- **Query Safety:** Uses parameterized queries (`?` placeholders) — **no SQL injection risk**

- **Severity:** **INFO**

---

## 7. Security Audit

| Check | Result |
|-------|--------|
| Hardcoded secrets | ❌ None found |
| SQL injection risk | ❌ None (parameterized queries) |
| f-strings in SQL | ❌ None |
| Circular imports | ❌ None |
| Module compilation | ✅ All 11 modules compile successfully |

- **Severity:** **INFO**

---

## 8. Code Quality Issues

### Critical
| Issue | File | Details |
|-------|------|---------|
| None | — | — |

### Warnings
| Issue | File | Details |
|-------|------|---------|
| `datetime.utcnow()` deprecated | 10+ files | `DeprecationWarning` in Python 3.12+. Should use `datetime.now(timezone.utc)` or `datetime.now(UTC)` |
| Untested integration functions | Multiple | Core methods like `search_text`, `search_companies`, `enrich`, `run` have no direct tests (only unit-tested helpers) |
| Telegram delivery untested | `exporters/telegram.py` | All 4 methods beyond `__init__` lack tests |
| Excel dtype coercion | `exporters/excel.py` | String columns become `float64` when all values are NaN |

### Info
| Issue | File | Details |
|-------|------|---------|
| No version control | Whole repo | No commits established |
| Dashboard type hints | `dashboard/app.py` | Return type doesn't match actual return value |
| `load_data` tuple unpacking | `dashboard/app.py` | Risk of `AttributeError` if `load_data` returns empty tuple unexpectedly |

---

## 9. New Code Without Tests

The following source files have **no dedicated test coverage** for their public methods:

| File | Untested Public Methods |
|------|------------------------|
| `compliance/robots_checker.py` | `check_url`, `get_delay`, `_fetch_robots`, `_parse_robots`, `_evaluate_url` |
| `compliance/consent_classifier.py` | `classify_email`, `classify_emails`, `get_consent_status` |
| `compliance/audit_logger.py` | `get_audit_for_record` |
| `discovery/google_places.py` | `search_text`, `get_details`, `nearby_search`, `search_maps_scrape`, `_make_request`, `_scrape_place_details` |
| `discovery/companies_house.py` | `search_companies`, `get_company_details`, `get_company_officers`, `get_filing_history`, `search_ecommerce_companies`, `_make_request` |
| `enrichment/website_analyzer.py` | `enrich`, `_respectful_get`, `_check_mobile`, `_detect_platform`, `_extract_social`, `_estimate_products`, `_check_content_freshness`, `_check_checkout` |
| `exporters/telegram.py` | `send_batch`, `send_batch_sync`, `send_notification`, `send_notification_sync` |
| `pipeline.py` | `run`, `main` |

> **Note:** Tests exist for the modules, but they only test helper/utility methods. Integration-level methods (the ones that actually call external APIs or orchestrate the pipeline) are untested.

---

## 10. Recommendations

### Immediate (Fix Before Release)
1. **Fix 3 failing tests** — all are test assertion bugs, not code bugs. Estimated time: 15 minutes.
2. **Replace `datetime.utcnow()`** with `datetime.now(timezone.utc)` across all modules to eliminate 38 deprecation warnings.

### Short Term
3. **Add integration tests** with `unittest.mock`/`responses` for API collectors (Google Places, Companies House, PageSpeed).
4. **Fix Excel exporter dtype handling** — use `dtype=object` or explicit string conversion to prevent `float64` coercion for ID/country columns.
5. **Fix dashboard type hints** and edge-case handling for empty data.

### Long Term
6. **Add async support** for I/O-bound operations to improve throughput.
7. **Establish git repository** with initial commit and CI/CD pipeline for automated testing.
8. **Add pytest-cov** to measure actual code coverage (currently unavailable in environment).

---

## Overall Assessment

| Category | Score | Notes |
|----------|-------|-------|
| Test Pass Rate | ⚠️ 96.8% | 3 test bugs to fix |
| Code Security | ✅ Good | No injection risks, no secrets |
| Performance | ✅ Good | No leaks, reasonable memory usage |
| Error Handling | ✅ Good | Graceful degradation without API keys |
| Code Coverage | ⚠️ Partial | Unit tests exist but integration coverage weak |
| Documentation | ✅ Good | Docstrings present throughout |

**Overall Status:** ✅ **Ready for development use** with minor test fixes recommended before production deployment.
